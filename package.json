{
  "name": "crud-clothing",
  "version": "1.0.0",
  "scripts": {
    "server": "json-server --watch server/db.json --delay 2000 --port 4000"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
